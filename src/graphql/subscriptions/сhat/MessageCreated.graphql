subscription MessageCreated($chatId: String!) {
  messageCreated(chatId: $chatId) {
    id
    content
    images
    isReadByOtherUser
    createdAt
    updatedAt
    user {
      id
      username
      name
      avatar
    }
  }
}

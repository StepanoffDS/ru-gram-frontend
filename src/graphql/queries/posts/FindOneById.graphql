query FindOneById($id: String!) {
  findOneById(id: $id) {
    id
    title
    text
    images
    hidden
    createdAt
    updatedAt
    isLiked
    likes
    commentsCount
    isMyPost
    user {
      id
      username
      name
      avatar
      role
    }
  }
}

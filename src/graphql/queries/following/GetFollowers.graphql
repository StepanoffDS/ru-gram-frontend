query GetFollowers($userId: String!, $skip: Int, $take: Int) {
  getFollowers(userId: $userId, skip: $skip, take: $take) {
    data {
      id
      username
      name
      avatar
      bio
      followedAt
    }
    total
    skip
    take
    hasMore
  }
}
